# MiNiMail

## 发布前检查

发布或打包正式测试包前，请先运行：

```bash
npm run test:release
```

该命令会先执行生产构建，再运行发布前关键回归测试，覆盖 AI Key 安全存储、远程图片拦截、草稿与缓存、正文预取、正文搜索、HTML 翻译保真、写邮件 AI/草稿、多语言、备份取消清理、通知与 Electron sandbox 等链路。

如果 `test:release` 失败，不要跳过失败项；需要先判断是代码回归还是测试断言过期，再决定修代码或最小更新测试。
