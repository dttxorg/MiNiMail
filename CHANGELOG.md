# Changelog

## v0.1.2 - 2026-05-01

### 功能更新摘要

本次更新完善了 MiNiMail 的写信、定时发送和发送后显示流程。用户现在可以使用增强后的富文本编辑器撰写邮件，安排本地定时发送，并在发送成功后立刻在 Sent 视图和联系人聚合正文中看到邮件内容。

### 定时发送

- 新增本地定时发送任务调度能力，支持到点自动通过 SMTP 发送。
- 定时发送任务在主进程中管理，使用数据库状态避免重复发送。
- 发送前会先写入本地 `sending` 状态，发送成功后更新为 `sent`，失败后更新为 `failed`。
- App 启动后会恢复 scheduled/missed/failed 任务状态，missed 任务不会静默发送。
- 用户可以在 Scheduled / 待发送视图查看任务状态、倒计时、详情，并取消或重试任务。
- 定时发送成功后会刷新 Sent 视图，并将邮件正文加入联系人聚合会话。

### 富文本编辑器

- Compose 富文本编辑器支持字体、字号、颜色、加粗、斜体、下划线、列表、对齐、插入链接和插入图片。
- 字体菜单新增 Arial、Times New Roman、Courier New、Verdana、Tahoma、Georgia、Trebuchet MS、Helvetica 等常见字体。
- 字号菜单新增 Normal、8px、10px、12px、14px、16px、18px、20px、24px、28px、32px、36px、48px。
- Normal 字号使用 14px 作为默认显示尺寸。
- 富文本发送继续使用 inline style，提高 Gmail、Outlook、Mail.app 等邮件客户端中的格式兼容性。

### 对齐和可访问性

- 对齐菜单支持左对齐、居中、右对齐和两端对齐。
- 字体、字号、颜色、对齐、加粗、斜体、下划线、列表、链接和图片控件补充 `title` 与 `aria-label`。

### 邮件显示和列表刷新

- 修复定时发送成功后邮件没有出现在 Sent 列表的问题。
- 修复定时发送成功后联系人聚合正文中看不到已发送邮件正文的问题。
- 修复发送后邮件标题、发件人和正文显示不完整的问题。
- Sent 本地缓存会保留 subject、from、to、bodyText、bodyHtml、localSendId 和附件元数据。

### 发送提示

- 普通发送仍保留 5 秒撤回机制。
- 发送提示现在会实时倒计时，例如从“邮件将在 5 秒后发送”更新到 4、3、2、1 秒。

### 测试覆盖

- 更新富文本编辑器回归测试。
- 更新定时发送、自动到点发送、Sent 缓存和联系人聚合显示回归测试。
- 更新发送撤回倒计时回归测试。
- 发布前通过 `npm run test:release` 和 `git diff --check`。
